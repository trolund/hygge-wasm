let f_outer: () -> (int, int) -> int = fun() -> {
    let mutable a: int = 3;
    let f_inner: (int, int) -> int = fun(x: int, y: int) -> {
        x + y + 2 + a
    };

    f_inner
};

let inner: (int, int) -> int  = f_outer();

assert(inner(1, 2) = 7)