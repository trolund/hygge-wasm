let factorial: (int) -> int = fun(n: int) -> {
    // Recursive tail calls 
    fun aux(n: int, acc: int): int = {
        if n = 0 then acc else aux(n - 1, n * acc)
    };

    aux(n, 1)
};

assert(factorial(0) = 1);
assert(factorial(5) = 120);
assert(factorial(10) = 3628800)