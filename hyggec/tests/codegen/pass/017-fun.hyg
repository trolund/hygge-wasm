fun f(x: int): int = x + x;
let g: (int) -> int = f;
let r: int = g(2);
assert(r = 4);

fun fx(x: int): int = x * x;
let gx: (int) -> int = fx;
let r1: int = gx(8);
assert(r1 = 64);

let gxs: (int) -> int = f;
let r2: int = gxs(8);
assert(r2 = 16)
