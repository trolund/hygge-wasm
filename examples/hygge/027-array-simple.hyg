let arr: array {int} = array(2 + 2, 40 + 2);

let len: int = arrayLength(arr);

let val: int = arrayElem(arr, 1);

assert(len = 4);
assert(val = 42);

arrayElem(arr, 1) <- 200;

let val2: int = arrayElem(arr, 1);

assert(val2 = 200)